org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
android.uniquePackageNames=false
android.r8.strictFullModeForKeepRules=false
# TODO(built-in-kotlin): these two opt-outs are deprecated in AGP 9 and removed in
# AGP 10. They exist because the project still applies the standalone Kotlin Gradle
# plugin (org.jetbrains.kotlin.android 2.2.10), which calls the legacy variant API.
# Migrating to AGP's built-in Kotlin (https://developer.android.com/r/tools/built-in-kotlin)
# lets us delete both lines.
android.builtInKotlin=false
android.newDsl=false
